"""sovereign — a self-hosted, data-sovereign AI platform for engineering teams.

This top-level package holds cross-cutting shared code (version, settings,
common types). Each capability lives in its own top-level package: ``gateway``,
``mcp``, ``rag``, ``eval``, ``ci``, ``adoption``. See README.md for the map of
how the pieces fit together.
"""

__version__ = "0.0.0"
