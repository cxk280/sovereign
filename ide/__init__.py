"""ide — developer tooling backed by the internal gateway endpoint.

The `sov` CLI (chat / review / test from the terminal), plus editor configs
(Continue, Cursor) and a minimal VS Code extension that use the gateway's
OpenAI-compatible API — a private, in-house alternative to cloud coding
assistants.
"""
