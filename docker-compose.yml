# Local ($0) stack for sovereign. Grows one service per build-order step.
# Step 0 ships the vector store (Qdrant) so RAG/MCP have a target in Step 2.
# Step 1 adds `gateway`; Step 1/2 add a local model backend (Ollama or llama.cpp).
services:
  qdrant:
    image: qdrant/qdrant:v1.12.4
    ports:
      - "6333:6333"   # REST
      - "6334:6334"   # gRPC
    volumes:
      - ./qdrant_storage:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c ':> /dev/tcp/127.0.0.1/6333' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --- added in Step 1 ---
  # gateway:
  #   build: ./gateway
  #   env_file: .env
  #   ports: ["8080:8080"]
  #   depends_on: [qdrant]

  # --- local model backend (Step 1); Ollama is the default $0 path ---
  # ollama:
  #   image: ollama/ollama:latest
  #   ports: ["11434:11434"]
  #   volumes: ["./models:/root/.ollama"]
