"""adoption — measure whether the platform is actually helping.

Aggregates the gateway's Prometheus metrics and IDE/CI acceptance signals into an
impact report (requests by task, acceptance rate, estimated hours saved) that
feeds the dashboard's Adoption view and a Grafana dashboard.
"""
